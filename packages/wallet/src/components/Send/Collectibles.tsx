import React from 'react'

export const SendCollectibles = () => {
  // Todo
  // Select asset (start with collection, then tokenid)
  // to field (validate address)
  // amount field (validate amount) (a max button could be cool)
  // send / cancel

  return (
    <div>
      <h1>Collectibles</h1>
    </div>
  )
}